---
title: "09.07 시스템 구조 정리"
date: 2026-09-07 00:00:00 +0000
categories:
  - "유니티"
tags:
  - "project"
  - "유니티"
---

### 프로젝트 시작
`Boot`씬에서 전역 시스템을 만든 뒤 Title로 넘기는 구조
```
실행
→ Boot 씬
→ __Core 전역 오브젝ㅌ트 초기화, 유지
→ DataRegistry 초기화
→ Title 씬 비동기 로드
```

`CoreBootstrapper.cs`
Unity가 첫 씬을 로드한 직후 `Initialize()` 가 자동으로 호출된다.

```C#
[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.AfterSceneLoad)]
private static void Initialize()
{
	EnsureInitialized();
}
```

`__Core`라는 오브젝트를 찾거나 만들고, `DontDestoryOnLoad`로 씬 전환 후에도 남도록 한다.

```C#
 public static void EnsureInitialized()
{
	if (initialized)
	{
		return;
	}

	GameManager gameManager = Object.FindAnyObjectByType<GameManager>();
	GameObject core = ResolveCoreHost(gameManager);
	EnsureCoreServices(core);
	RemoveLegacySettingsManagers(core);

	if (gameManager == null)
	{
		core.AddComponent<GameManager>();
	}

	Object.DontDestroyOnLoad(core);
	initialized = true;
}
```

그 아래에 씬 전환, 데이터, UI, 오디오, 인벤토리, 대화, 퍼즐 진행, 설정 등의 전역 서비스를 보장한다.
```C#
private static void EnsureCoreServices(GameObject core)
    {
        EnsureComponent<SceneFlowManager>(core, "SceneFlow");
        EnsureComponent<DataRegistry>(core, "Data");
        EnsureComponent<ResourceManager>(core, "Resources");
        EnsureComponent<UIManager>(core, "UI");
        EnsureComponent<CursorManager>(core, "UI");
        EnsureComponent<AudioManager>(core, "Audio");
        EnsureComponent<InventoryManager>(core, "Inventory");
        EnsureComponent<AlbumCollection>(core, "Album");
        EnsureComponent<PuzzleProgressManager>(core, "Progress");
        EnsureComponent<DialogueManager>(core, "Dialogue");
        EnsureComponent<SequenceRunner>(core, "Sequence");
        EnsureComponent<SettingsManager>(core, "Settings");
    }
```
이미 있는 서비스는 그대로 사용하고 없는 경우에 런타임에서 추가한다.

```C#
private static T EnsureComponent<T>(GameObject core, string systemName) where T : Component
{
	T existingComponent = core.GetComponentInChildren<T>(true);
	if (existingComponent != null)
	{
		return existingComponent;
	}

	GameObject systemObject = EnsureSystemObject(core, systemName);
	return systemObject.AddComponent<T>();
}

private static GameObject EnsureSystemObject(GameObject core, string systemName)
{
	Transform systemTransform = core.transform.Find(systemName);
	if (systemTransform != null)
	{
		return systemTransform.gameObject;
	}

	GameObject systemObject = new GameObject(systemName);
	systemObject.transform.SetParent(core.transform);
	return systemObject;
}
```

`GameManagers.cs`
시스템을 이용해서 게임의 현재 상태를 결정하는 곳
```C#
private void Awake()
{
	if (Instance != null && Instance != this)
	{
		Destroy(gameObject);
		return;
	}

	Instance = this;
	DontDestroyOnLoad(gameObject);
	CoreBootstrapper.EnsureServices(gameObject);
	GameServices.ValidateCoreServices();
	GameServices.Data.Initialize();
	SetState(initialState);
}
```
`Awake`에서 싱글턴 등록, 전역 서비스 준비, 검증, 데이터 초기화를 진행한다.
```C#
private IEnumerator Start()
{
	yield return null;
	
	...
}
```
한 프레임을 기다렸다가 Start()가 돌아가도록 함.
`Awake()`에서 `__Core`와 전역 서비스를 만들고 데이터를 초기화한 뒤, 다른 컴포넌트의 초기화도 진행될 시간을 준 다음 Title 전환을 시작하려는 의도


`SceneFlowManager.cs`
전환 중 상태, 페이드, UI, 오디오를 함께 관리하는 화면 흐름 담당자

```
씬 전환 요청
→ IsLoading 검사
→ GameState를 Cutscene으로 전환
→ 검은 페이드 인
→ LoadSceneAsync 시작, 씬 활성화 보류
→ 로딩 오버레이 표시 후 페이드 아웃
→ progress가 0.9에 도달할 때까지 대기
→ 다시 검은 페이드 인
→ 새 씬 활성화
→ GameState, BGM 적용, 로딩 UI 숨김
→ 검은 페이드 아웃
```

---

### ClassDiagram
```mermaid
classDiagram
    direction LR

    class GameServices {
        <<static facade>>
        +Inventory
        +PuzzleProgress
        +UI
        +Audio
    }

    class ThirdPersonController {
        +Move()
        +JumpAndGravity()
    }
    class StarterAssetsInputs
    class PlayerStateController {
        +SetBaseState()
        +RequestActionState()
        +RequestLockState()
    }

    class MouseInteractionController {
        +TryInteract()
    }
    class PlayerCharacterInteractionController {
        +TryInteract()
    }
    class IInteractionTarget {
        <<interface>>
        +InteractionPrompt
        +InteractionTransform
    }
    class IMouseInteractable {
        <<interface>>
        +CanMouseInteract()
        +MouseInteract()
    }
    class IPlayerCharacterInteractable {
        <<interface>>
        +CanPlayerInteract()
        +PlayerInteract()
    }

    class FlowInteractableBase {
        +PassesFlowCheck()
        +PublishFlowInteraction()
    }
    class InteractableObject
    class ItemPickupInteractable
    class PuzzleInteractable

    class InventoryManager {
        +AddItem()
        +HasRequirements()
        +TryConsumeRequirements()
    }
    class ItemDefinition

    class PuzzleController {
        +CanOpenPuzzle()
        +OpenPuzzle()
    }
    class PuzzleDefinition
    class IPuzzleRunner {
        <<interface>>
        +CanOpen()
        +Open()
    }
    class PuzzlePopupBase
    class PuzzleProgressManager

    class FlowSystem {
        +TryCheckInteraction()
    }
    class FlowDefinition
    class FlowRule

    class EventBus {
        <<static>>
        +Publish()
        +Subscribe()
    }
    class DialogueManager {
        +Play()
        +Advance()
    }
    class DialogueDatabase
    class SequenceRunner {
        +Play()
    }
    class EventSequenceAsset

    ThirdPersonController --> StarterAssetsInputs : 입력을 읽음
    ThirdPersonController --> PlayerStateController : 이동 상태 갱신

    IInteractionTarget <|-- IMouseInteractable
    IInteractionTarget <|-- IPlayerCharacterInteractable
    IMouseInteractable <|.. InteractableObject
    IPlayerCharacterInteractable <|.. InteractableObject
    IMouseInteractable <|.. ItemPickupInteractable
    IPlayerCharacterInteractable <|.. ItemPickupInteractable
    IMouseInteractable <|.. PuzzleInteractable
    IPlayerCharacterInteractable <|.. PuzzleInteractable

    MouseInteractionController ..> IMouseInteractable : 감지·실행
    PlayerCharacterInteractionController ..> IPlayerCharacterInteractable : 감지·실행

    FlowInteractableBase <|-- InteractableObject
    FlowInteractableBase <|-- PuzzleInteractable
    FlowInteractableBase ..> FlowSystem : 진행 조건 확인

    ItemPickupInteractable ..> InventoryManager : 아이템 추가
    InteractableObject ..> InventoryManager : 요구 아이템 검사·소비
    InventoryManager --> ItemDefinition

    PuzzleInteractable --> PuzzleController : 퍼즐 열기 요청
    PuzzleController --> PuzzleDefinition
    PuzzleController ..> IPuzzleRunner : 실행 위임
    PuzzleController ..> PuzzlePopupBase : 퍼즐 UI 표시
    PuzzleController ..> PuzzleProgressManager : 진행 저장

    FlowSystem --> FlowDefinition
    FlowDefinition --> FlowRule

    EventBus ..> FlowSystem : 이벤트 전달
    EventBus ..> DialogueManager : 대사 요청
    EventBus ..> SequenceRunner : 연출 요청

    DialogueManager --> DialogueDatabase
    SequenceRunner --> EventSequenceAsset

    GameServices ..> InventoryManager
    GameServices ..> PuzzleProgressManager
```


입력 구조가 키보드 마우스로 분리 되어있고, PC에서만 적용 가능함.
-> Input System을 활용해서 패드도 바인딩할 수 있도록 구조 변경

```mermaid
classDiagram
    direction LR

    class PlayerInput {
        <<Unity Input System>>
        Interact 액션
    }

    class InteractionInputRouter {
        <<new>>
        +OnInteract(context)
        +SelectProvider(source)
    }

    class IInteractionTargetProvider {
        <<interface>>
        +TryGetCandidate(candidate) bool
    }

    class MouseInteractionController {
        현재 마우스 위치 Raycast
        커서·하이라이트
    }

    class PlayerCharacterInteractionController {
        플레이어 전방 SphereCast
        근접 대상·패드 하이라이트
    }

    class InteractionCandidate {
        +IInteractable Target
        +Transform Transform
        +Vector3 HitPoint
    }

    class IInteractable {
        <<interface>>
        +CanInteract(context) bool
        +Interact(context) InteractionResult
        +InteractionPrompt
    }

    class InteractionContext {
        +GameObject Actor
        +InteractionSource Source
        +Vector3 HitPoint
    }

    class InteractionSource {
        <<enumeration>>
        Mouse
        Keyboard
        Gamepad
    }

    class InteractableObject
    class ItemPickupInteractable
    class PuzzleInteractable

    class FlowSystem
    class InventoryManager
    class PuzzleController

    PlayerInput --> InteractionInputRouter : Interact 입력
    InteractionInputRouter --> IInteractionTargetProvider : 대상 요청
    IInteractionTargetProvider <|.. MouseInteractionController
    IInteractionTargetProvider <|.. PlayerCharacterInteractionController
    IInteractionTargetProvider --> InteractionCandidate

    InteractionInputRouter --> IInteractable : 한 번만 실행
    InteractionInputRouter --> InteractionContext

    IInteractable <|.. InteractableObject
    IInteractable <|.. ItemPickupInteractable
    IInteractable <|.. PuzzleInteractable

    InteractableObject --> FlowSystem : 조건 검사·이벤트
    InteractableObject --> InventoryManager : 요구 아이템
    ItemPickupInteractable --> InventoryManager : 아이템 획득
    PuzzleInteractable --> PuzzleController : 퍼즐 열기
```

---
