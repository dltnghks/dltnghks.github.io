---
title: "04.14 Progress"
date: 2026-04-14 00:00:00 +0000
categories:
  - "졸업논문"
tags:
  - "project"
  - "졸업논문"
---

검은 신화 오공에 비해 F1 score가 굉장히 낫게 나왔다.
1. 검은 신화 오공의 5개 클립이 신뢰도가 낮다
2. 피의 거짓 클립 12개도 신뢰도가 낫다

각 게임 별 신뢰도를 일단 높이는게 우선
적어도 30개 이상의 경계는 필요 -> P의 거짓 데이터에서는 한 클립이 10개 이상의 클립이 나옴

P의 거짓
- [x] ZmlZCMn7Jjw
- [x] -20XxjROepI
- [x] *UNU6EsmdbZk*

검은 신화 오공
- [x] _R5IdnlZ0R4
- [x] *T4DFKOlrXCQ*
- [x] uUjZWYR-Mfo

엘든링
- [x] bg8i4CvE74M
- [x] rTB7oVYog70
- [x] tmxoXm9uVPI

---

일단 3개로 F1 score 찾기

DDM-net 돌리는데 GPU를 안쓰는거 같음

**1. PNG → JPEG 변환** JPEG 디코딩이 PNG보다 3~5배 빠릅니다. 프레임을 처음부터 JPEG로 추출하거나, 기존 PNG를 변환하면 DataLoader I/O 병목이 대폭 줄어듭니다.

```bash
# 기존 PNG → JPEG 일괄 변환 (mogrify 사용)
mogrify -format jpg -quality 95 {clip_dir}/img_*.png && rm {clip_dir}/img_*.png
```

다만 `SingleVideoDataset`이 자동으로 확장자를 감지하므로 변환 후 스크립트 수정 없이 바로 적용됩니다.

**2. num_workers 증가 (4 → 8~12)** [run_ddm_zml.py:122](vscode-webview://0irhj5rbt8p44ssb7h3omaiq5b9eenaj8ect63j5cmruls6q0ufc/scripts/gebd/run_ddm_zml.py#L122)

```python
loader = DataLoader(dataset, batch_size=32, shuffle=False,
                    num_workers=8, pin_memory=True, ...)
```

**3. batch_size 증가 (32 → 128)** TITAN RTX 24GB면 충분합니다. GPU 유휴 시간을 줄여줍니다.

```python
loader = DataLoader(dataset, batch_size=128, ...)
```

**4. 두 GPU 병렬 활용** 클립을 절반씩 나눠 `CUDA_VISIBLE_DEVICES=0`, `CUDA_VISIBLE_DEVICES=1`로 동시 실행 → ~2배 속도.

이거 세팅하려고 프로세스 강제 종료했더니 GPU 연결 오류가 난 듯
nvidia-smi 입력 시
Unable to determine the device handle for GPU0000:65:00.0: Unknown Error
에러 발생
