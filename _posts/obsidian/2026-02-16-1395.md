---
title: "1395 (스위치)"
date: 2026-02-16 05:34:52 +0000
categories:
  - "Baekjoon"
tags:
  - "baekjoon"
  - "daily"
  - "project"
---

# 2/15 (일) - 세그트리 복습 + 확장
![Pasted image 20260215161345](/assets/img/obsidian/01.Project/02.Baekjoon/02.Problems/Pasted%20image%2020260215161345-6a614389.png)
## 오늘 문제
- [[1395 (스위치)]]

## 오늘 목표
- 핵심 아이디어를 스스로 설명할 수 있을 정도로 정리
- 풀이 후 시간복잡도/병목 구간 점검

## 체크리스트
- [x] 1회 정답 제출
- [x] 핵심 로직 3줄 요약
- [x] 오답/실수 포인트 기록

## 풀이 메모
- lazy 배열을 통해 반전 횟수를 저장해둔다.
- 세그먼트 트리를 통해 범위 내에 활성화된 스위치 개수를 저장해둔다.
	- 짝수번 반전된 스위치는 그대로 둔다.
	- 홀수번 반전된 스위치는 (범위 - 현재 활성화된 스위치 수)로 반전시켜준다.
- Solve 과정에서도 탐색하는 범위에 대해 lazy 처리를 진행해준다.

```c++
#include <iostream>
#include <vector>

using namespace std;

int n, m;
bool switchList[100001];
int tree[100000 * 4 + 1];

// 반전된 횟수를 기록
int lazy[100000 * 4 + 1];

void Push(int cur, int left, int right){
    int size = (right - left + 1);
    // 반전횟수가 홀수일 때만 반전
    if((lazy[cur] % 2) != 0)
        tree[cur] = (size - tree[cur]);

    // 자식들은? 반전 횟수 추가
    if(left != right){
        lazy[cur * 2] += lazy[cur];
        lazy[cur * 2 + 1] += lazy[cur];
    }

    // 반전이 끝난 cur은 lazy값을 0으로 설정
    lazy[cur] = 0;
}

void Reverse(int cur, int left, int right, int targetL, int targetR){
    //cout << cur << '\n';
    Push(cur, left, right);

    if(left > targetR || right < targetL){
        return;
    }

    if(left >= targetL && right <= targetR){
        lazy[cur]++;
        Push(cur, left, right);
        return;
    }

    int mid = (left+right) / 2;
    Reverse(cur * 2, left, mid, targetL, targetR);
    Reverse(cur * 2 + 1, mid+1, right, targetL, targetR);
    tree[cur] = tree[cur * 2] + tree[cur * 2 + 1];
}

int Solve(int cur, int left, int right, int targetL, int targetR){
    Push(cur, left, right);

    if(left > targetR || right < targetL){
        return 0;
    }

    if (left >= targetL && right <= targetR) {
        return tree[cur];
    }

    int mid = (left+right) / 2;
    int res = Solve(cur * 2, left, mid, targetL, targetR) + Solve(cur * 2 + 1, mid+1, right, targetL, targetR);
    return res;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> n >> m;

    for(int i = 0; i < m; i++){
        int o, s, t;
        cin >> o >> s >> t;

        int size = t - s + 1;
        // o == 0 s~t 스위치 반전
        if(o == 0){
            Reverse(1, 1, n, s, t);
        }
        // o == 1 s~t 켜져있는 스위치 개수 확인
        else if(o == 1){
            cout << Solve(1, 1, n, s, t) << '\n';
        }
    }

    return 0;
}

```

## 회고
- 작은 범위를 가지고 예상 동작을 그려본 후 코드를 작성하면 더 손쉽게 작성할 수 있다.
- 여기서는 예제 입력이 4개로 깔끔하게 이진트리가 구성되고 이걸 가지고 그림을 그려봤다.

